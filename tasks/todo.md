## 2026-03-07 修复 uTools 新建书签标题描述文件缺失

- [x] 梳理 `utools` 模式下新建书签的数据流与文件写入链路，定位标题/描述未落盘的根因
- [x] 实现最小修复，确保新建书签时正确获取并写入标题/描述文件
- [x] 运行 `bun run build` 验证并确认 `dist` 已更新

## Review

- 变更（本次）：`uTools` 内置浏览器抓取分支现在会同时提取页面 `title` 与 `meta description`，不再只返回图标。
- 兜底（本次）：即使 favicon 抓取失败，只要拿到了页面元数据，也会回传给新建表单用于自动填充标题和描述。
- 验证（本次）：`bun run build` 通过，并重新生成了 `dist` 发布产物。

## 2026-03-01 路径提示改为一次性

- [x] 将 `local-mode-device-path` 提示策略改为一次性展示
- [x] 移除路径提示冷却相关状态与逻辑，避免策略冲突
- [x] 运行 `bun run build` 验证并更新 `dist`
