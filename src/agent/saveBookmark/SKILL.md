# 自动整理并保存书签

## 适用需求

- 用户只提供网址并要求自动保存。
- 一次生成标题、简介和一到三个分类位置。

## 输入检查

- 必须有网址。
- 页面标题和描述可以为空。
- 分类只能来自用户提供的分组列表。

## 保存规则

- 标题不超过 15 个汉字。
- 简介不超过 40 个汉字。
- 默认只选一个最贴切的位置。
- 仅在网址明确跨领域时增加分类。
- 无法可靠分类时不指定位置，由客户端使用快速收集。
- 生成元信息和分类后调用 `proposeChanges`，等待用户在界面确认。

## 完成

- 提案生成后说明尚未写入，并等待用户确认。
